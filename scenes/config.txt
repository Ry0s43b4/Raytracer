# Configuration of the camera
camera:
{
    resolution = { width = 800; height = 600; };
    position = { x = 0.0; y = 0.0; z = 0.0; };
    rotation = { x = 0.0; y = 0.0; z = 0.0; };
    fieldOfView = 72.0;
};

# Primitives in the scene
primitives:
{
    spheres = (
        # Red sphere, front and center
        { x = 0.0; y = 0.0; z = 5.0; r = 1.0; color = { r = 255; g = 64; b = 64; }; },
        # Green sphere, to the left
        { x = -2.5; y = 0.0; z = 6.0; r = 1.0; color = { r = 64; g = 255; b = 64; }; },
        # Blue sphere, to the right
        { x = 2.5; y = 0.0; z = 6.0; r = 1.0; color = { r = 64; g = 64; b = 255; }; }
    );

    planes = (
        # Floor plane on Z axis, sitting below the spheres
        { axis = "Y"; position = -1.0; color = { r = 200; g = 200; b = 200; }; }
    );

    cylinders = (
        { x = 4.0; y = 0.0; z = 6.0; axis = "Y"; r = 0.2; color = { r = 0; g = 0; b = 255; }; },
    )
};

# Light configuration
lights:
{
    ambient = 0.3;
    diffuse = 0.7;

    point = ();

    directional = (
        # Light coming from upper-left-front
        { x = -1.0; y = -1.0; z = 1.0; }
    );
};